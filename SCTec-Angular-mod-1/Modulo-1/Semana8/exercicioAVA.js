const botao = document.querySelector("#botao");
const mensagem = document.querySelector("#mensagem");

// botao.addEventListener("click", () => { mensagem.textContent = "Olá!"; });
const campoNome = document.querySelector("#nome");

// campoNome.addEventListener("click", () => { console.log("Tecla pressionada"); });

// campoNome.addEventListener("keyboard", () => { console.log("Tecla pressionada"); });

// campoNome.addEventListener("keydown", () => { console.log("Tecla pressionada"); });
